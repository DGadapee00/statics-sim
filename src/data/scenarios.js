/**
 * Scenario fallback. Every statics lab defines its own `applyScenario`, so this only exists for
 * the shared simbridge path and is a no-op here.
 */
export function applyScenario() {}
export const SCENARIOS = {};
